# Example Markdown Book

This is a small BookOps fixture for testing GitHub Markdown imports.

It uses the numbered-folder convention:

- `01-getting-started`
- `02-going-deeper`

Each folder contains ordered chapter files. The content is intentionally simple so import failures are easy to diagnose.
